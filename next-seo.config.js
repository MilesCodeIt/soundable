/** @type {import("next-seo").DefaultSeoProps} */
const SeoProps = {
  defaultTitle: "Soundable",
  titleTemplate: "%s | Soundable",
  additionalLinkTags: [
    {
      rel: "icon",
      href: "favicon.ico"
    }
  ]
};

export default SeoProps;